int n = 5;
		int nst = 1;
		int row = 1;
		while(row <= n ) {
			int cst = 1;
			
			while(cst <= nst) {
				if(cst==1 || cst==nst)
					System.out.print(row);
					else 
						System.out.print("0");
				cst++;
			}
			System.out.println();
			row++;
			nst++;
		}
